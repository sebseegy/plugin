# monday-formulas — confirmed release changelog

Dated log of confirmed Formula column changes, newest entry first, fed
automatically by `weekly-pulse` each time it runs. Covers new supported
functions, changes to existing function behaviour, expanded column-type support
(e.g. formula now works on mirror/connect-boards columns), and confirmed bugs
or regressions in formula evaluation.

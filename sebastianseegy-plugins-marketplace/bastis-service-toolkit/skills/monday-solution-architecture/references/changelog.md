# monday-solution-architecture — confirmed release changelog

Dated log of platform releases relevant to solution design, newest entry first,
fed automatically by `weekly-pulse` each time it runs. Covers new column types,
board-level features (hidden columns, import changes, view improvements),
cross-board capabilities, integration releases, and anything that changes how
a solution should be architected — including features that lift previous
workarounds or constraints.
